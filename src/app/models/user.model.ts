export interface User {
    id?: number; // generado automáticamente por json-server*
    usuario: string;
    password: string;
    nombre: string;
    apellidop: string;
    apellidom: string;
    email: string;
}
